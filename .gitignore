# Built output — CI regenerates all of this on every push
site/
_site/
slides/**/*.html

# Local preview (tools/preview.sh)
.venv-preview/
docs/slides/

# Local scratch
.DS_Store
__pycache__/
*.pyc
.claude/

# Local scratch only: the raw Learning Suite download. Course data that ships with the
# site lives under docs/ and is committed. Anchored so it cannot match docs/*/data/.
/data/
